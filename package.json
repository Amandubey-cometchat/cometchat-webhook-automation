{
  "name": "cometchat-webhook-automation",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "register": "node scripts/register-webhooks.js",
    "test:webhooks": "playwright test; code=$?; node scripts/upload-test-results.js; exit $code"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
