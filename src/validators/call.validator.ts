/**
 * No Calls webhook payload has ever been captured (see
 * src/registry/calls.registry.ts — BLOCKED, no Calls SDK integration
 * exists). Add validators here once that integration exists and a real
 * payload has been observed — never assume the shape.
 */
export const CALL_VALIDATORS_PENDING_SDK_INTEGRATION = true;
