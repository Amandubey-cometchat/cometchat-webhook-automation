/**
 * No Campaign webhook payload has ever been captured (see
 * src/registry/campaign.registry.ts — BLOCKED, no Campaigns module
 * integration exists). Add validators here once that integration exists and
 * a real payload has been observed — never assume the shape.
 */
export const CAMPAIGN_VALIDATORS_PENDING_MODULE_INTEGRATION = true;
